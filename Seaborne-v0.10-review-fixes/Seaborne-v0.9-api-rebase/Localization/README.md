# Seaborne Localization

These JSON files provide complete localisation coverage for the current Seaborne source tree.

The current code still exposes some display strings directly through `CustomCharacterModel`, `CustomPowerModel`, and `CustomRelicModel` overrides because public STS2/BaseLib examples use inline strings for these models. Keep these files as the source of truth when the exact BaseLib localisation loader/API is confirmed.
